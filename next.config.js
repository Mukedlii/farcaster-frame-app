/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you need to allow remote domains for images in frames, specify here.
  images: {
    domains: []
  }
};

module.exports = nextConfig;